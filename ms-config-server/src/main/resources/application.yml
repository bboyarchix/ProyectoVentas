server:
  port: 7070


spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/josephmiguel1103/microservice_Ecomerce.git # ruta del repositorio git
          searchPaths: config-data
          default-label: main
          #username:
          #password:

  security:
    user:
      name: root
      password: 123456